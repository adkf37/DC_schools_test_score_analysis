# Validation Report

**Date:** 2026-04-25  
**Reviewer:** Ralph  
**Recommendation:** **FAIL — return to Build**

## Scope

Validate the latest Task 03 / Task 05 build output against the backlog acceptance criteria and current sprint commitments.

## Checks Run

1. **Environment setup**
   - Command: `python -m pip install -r requirements.txt`
   - Result: ✅ Passed

2. **Python syntax sanity check**
   - Command: `python -m py_compile src/*.py app/*.py inspect_data.py`
   - Result: ✅ Passed

3. **Documented data pipeline smoke test**
   - Command: `python src/load_clean_data.py`
   - Result: ❌ Failed
   - Evidence:
     - `load_clean_data.py` looked for these exact top-level files:
       - `input_data/2021-22 School Level PARCC and MSAA Data.xlsx`
       - `input_data/2022-23 School Level PARCC and MSAA Data_9_5.xlsx`
       - `input_data/2023-24 School Level DCCAPE and MSAA Data 1.15.2025.xlsx`
       - `input_data/2024-25 Public File School Level DCCAPE and MSAA Data 1.xlsx`
     - The repo currently contains different workbook names under `input_data/School and Demographic Group Aggregation/`.
     - No exact match for the expected 2024-25 workbook was present in the repo snapshot.

4. **Cohort analysis smoke test**
   - Command: `python src/analyze_cohort_growth.py`
   - Result: ❌ Failed
   - Evidence:
     - Script exited because `output_data/combined_all_years.csv` was not generated by the failed loader step.

5. **Input-file contract check**
   - Command: compare expected loader filenames vs actual `input_data/**/*.xlsx`
   - Result: ❌ Failed
   - Evidence summary:
     - Actual files found:
       - `input_data/School and Demographic Group Aggregation/DC Cape Scores 2023-2024.xlsx`
       - `input_data/School and Demographic Group Aggregation/DC PARCC Scores – School Year 2021-22.xlsx`
       - `input_data/School and Demographic Group Aggregation/DC PARCC Scores – School Year 2022-23.xlsx`
       - plus older 2015-16 / 2016-17 / 2017-18 / 2018-19 workbooks
     - Missing exact matches for all four filenames hardcoded in `src/load_clean_data.py`

## Acceptance-Criteria Status

- **Task 03**
  - `python src/analyze_cohort_growth.py` exits with code 0 — ❌
  - Required cohort output files exist with expected row counts — ❌
  - Stuart-Hobson benchmark matches within ±0.1 pp — ⛔ Blocked

- **Task 05**
  - Significance test columns verified in generated outputs — ⛔ Blocked
  - Methods note exists (`docs/methods.md`) — ✅
  - Task 03 acceptance criteria still pass — ❌

## Blocked Checks

- Could not verify `combined_all_years.csv`, `cohort_growth_detail.csv`, `cohort_growth_summary.csv`, or `cohort_growth_pivot.xlsx` because the loader never completed.
- Could not run the Stuart-Hobson regression check because no cohort outputs were generated.
- Dashboard validation was not run because Task 04 remains pending and dashboard dependencies are still optional/commented in `requirements.txt`.

## Conclusion

Validation does **not** meet the backlog acceptance criteria. The code is syntactically valid, but the end-to-end pipeline is not currently runnable from a fresh clone because the loader's hardcoded input-file contract does not match the repo's present data layout and filenames. The next loop should return to **Build** to align data discovery (or data placement), generate `output_data/combined_all_years.csv`, and rerun validation.
